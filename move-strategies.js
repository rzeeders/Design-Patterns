/**
 * implementeer hier een paar strategies
 * allemaal met een functie: getNextPosition(currentPos)
 * waarbij:
 *  - currentPos een vector is
 *  - een nieuwe vector geretourneerd wordt
 *
 * in je Mover class (en diens subclasses) kan je dan in de constructor een strategy injecten, waarop de Mover in update() `getNextPosition(...)` kan aanroepen, 
 * zonder te weten welke implementatie het is.
 */